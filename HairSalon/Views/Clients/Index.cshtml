@{
  Layout = "_Layout";
}

@using HairSalon.Models;
<h1>Client List</h1>
@if (@Model.Count == 0)
{
  <h2>No clients assigned to Stylist.</h2>
}

<ul>
@foreach(Client client in Model)
{
  <li>@Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId})</li>

}
</ul>
<p>@Html.ActionLink("Add Clients", "Create")</p>